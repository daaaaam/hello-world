char* StrCpy(char* src)
{
int len=0;
len=strlen(const char* src);
int *ptr;
ptr=(int *)malloc(sizeof(char)*(len+1));
int i,j=0;
for(i=0;i<len;i++)
{
if(src[i]<'0'||src[j]>'9')
{
ptr[j]=src[i];
j++;}
}
while(gets(src)!=NULL)
{
char ptr[len+1]={'\0'};}
if(p!=NULL)
{
free(p);
p=NULL;}
return 0;}
